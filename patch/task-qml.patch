--- a/Task.qml	2026-09-15 16:45:20.740952881 +0200
+++ b/Task.qml	2026-09-15 16:45:20.774179133 +0200
@@ -15,7 +15,7 @@
 import org.kde.plasma.extras as PlasmaExtras
 import org.kde.plasma.components as PlasmaComponents3
 import org.kde.kirigami as Kirigami
-import plasma.applet.org.kde.plasma.taskmanager as TaskManagerApplet
+import "tmlocal" as TaskManagerApplet
 import org.kde.plasma.plasmoid
 
 import org.kde.taskmanager as TaskManager
@@ -237,7 +237,7 @@
 
     onSmartLauncherEnabledChanged: {
         if (smartLauncherEnabled && !smartLauncherItem) {
-            const component = Qt.createComponent("plasma.applet.org.kde.plasma.taskmanager", "SmartLauncherItem");
+            const component = Qt.createComponent("tmlocal/SmartLauncherItem.qml");
             const smartLauncher = component.createObject(task);
             component.destroy();
 
@@ -571,6 +571,29 @@
             enabled: true
 
             source: task.model.decoration
+
+            // Einfaerbung und Hover-Drehung, siehe plasma-icontasks-tint
+            transformOrigin: Item.Center
+            layer.enabled: true
+            layer.smooth: true
+            layer.effect: IconEffect {
+                hovered: task.highlighted
+                // Angeheftete Starter und geoeffnete Fenster lassen sich getrennt
+                // einfaerben. Im Nur-Symbole-Modus wird der Starter einer laufenden
+                // Anwendung ausgeblendet und durch ihr Fenster ersetzt, deshalb
+                // trennt IsLauncher hier genau "angeheftet" von "geoeffnet".
+                tintColor: task.model.IsLauncher
+                    ? Plasmoid.configuration.tintColor
+                    : Plasmoid.configuration.tintColorOpen
+                tintStrength: task.model.IsLauncher
+                    ? Plasmoid.configuration.tintStrength
+                    : Plasmoid.configuration.tintStrengthOpen
+            }
+
+            HoverSpin {
+                target: icon
+                hovered: task.highlighted
+            }
         }
 
         states: [
